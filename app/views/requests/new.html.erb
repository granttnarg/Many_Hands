<%= simple_form_for [@spot, @request] do |f| %>
  <%= f.input :status, collection: Request.statuses.keys %>
  <%= f.input :note %>
  <%= f.submit "Create Request", class: "btn btn-primary" %>

<% end %>
