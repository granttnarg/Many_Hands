<div class="container">
<%= simple_form_for([@spot, @invitation]) do |f| %>
  <%= f.input :message %>
  <%= f.input :creative_id, as: :hidden, input_html: { value: params[:creative_id] } %>
  <%= f.submit 'Invite', class: "btn btn-primary" %>
<% end %>

</div>
