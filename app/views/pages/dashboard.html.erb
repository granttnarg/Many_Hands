    <%= render 'shared/navbar' %>
    <br>
    <br>
    <br>
    <br>
  <div class="box">
    <div class="wrapping-box">

    <div class="first-row-box">
      <div class="upper-left-box">
        <div class ="cover-avatar">
          <%= cl_image_tag @user.profile_photo, crop: :fill, class: "circle-avatar" %>
        <div class="cover-photo">
          <%= cl_image_tag @user.coverphoto, height: 150, width: 500, crop: :fill, class: "cover-photo" %>
        </div>
      </div>
        <div class="notification-box">
          <div class="each-notification">
            <p class ="notification-text">CreativeA made a request to this event 1. </p>
          </div>
          <div class="each-notification">
            <p class ="notification-text">CreativeA made a request to this event 1. </p>
          </div>
          <div class="each-notification">
            <p class ="notification-text">CreativeA made a request to this event 1. </p>
          </div>
        </div>
      </div>

      <div class="upper-right-box">
          <%#= link_to "Create new event", new_event_path, class: "btn btn-primary" %>

        <div class="your-self">
          <h4 class ="profile-text"> Your Profile</h4>
         <div class="social-media">
            <i class="fab fa-facebook-f"></i>
            <i class="fab fa-instagram"></i>
            <i class="fab fa-youtube"></i>
         </div>
            <ul class ="profile-content">
            <li style="list-style: none;"><p class ="profile-text">Name: <%= @user.first_name  %> <%= @user.last_name  %></li></p>
            <li style="list-style: none;"><p class ="profile-text">Location: <%= @user.location  %></li></p>
            <li style="list-style: none;"><p class ="profile-text">Looking for: <%= @user.looking_for  %></li></p>
            <li style="list-style: none;"><p class ="profile-text">Contact: <%= @user.email  %></li></p>
           </ul>
        </div>
          <div class="create-new-event">

                <%= link_to "Create new event", new_event_path, class: "create-event" %>
                <svg id="arrow-dash" href="/events" viewBox="0 0 24 24">
                    <polygon points="1,0 1,6 13.757,6 0.615,19.142 4.858,23.385 18,10.243 18,23 24,23 24,0"></polygon>
                </svg>

          </div>
      </div>
    </div>
  <div class="row">
     <% @events.each do |event|  %>
       <div id = "each-event" class="col-6">
        <div class="event-info">
        <%= link_to event_path(event) do %>
        <div class="event-photo">
          <img id="dash-card" src="<%=event.photo%>" height="150" width="200"/>
         <% end %>
        </div>
        <div class="event-description">
          <h6 style="list-style: none;"><b> EVENT NAME: </b><%= event.name %></h6>
          <h6 style="list-style: none;">VENUE: <%= event.venue_name  %></h6>
          <h6 style="list-style: none;">DATE: <%= event.address  %></h6>
          <h6 style="list-style: none;">START: <%#= event.start_date.strftime("%b %d, %Y") %></h6>
          <h6 style="list-style: none;">End <%#= event.end_date.strftime("%b %d, %Y") %></h6>
            </div>
        <div class="spot-status">
          3 spots /
          2 filled<!--
        hard-coded please change with ruby code! -->
        </div>
        </div>
        <% event.spots.each do |spot| %>
          <% notifications = spot.requests.where(status: :pending) %> <%# Need to change to "pending" %>
          <% notifications.each_with_index do |notification, index| %>
        <div id="request-box">
          <p class ="request-text">Message # <%= index + 1 %> <br>
              From <%= link_to "#{spot.requests.first.user.first_name}", spot_creative_path(spot, spot.requests.first.user)%> <br> Status: <%= notification.status %></p>
              <h6 class = "category"><%= spot.category %>
                <%= spot.role %>
              </h6>
              <p class ="request-text">Content: <%= notification.note %></p>

                  <%= simple_form_for [@spot, notification], url: spot_request_path(spot, notification), html: { class: "status-form" } do |f| %>
                   <div class="request-status-box">
                  <a href="#" class="button" data-value="confirmed">✅</a>
                  <a href="#" class="button" data-value="rejected">❌</a>
<!--                  //  search button should go here -->
                  <%= f.input :status, as: :hidden, input_html: { class: 'status-input' } %>
                  <%= f.submit :submit, class: "submit d-none" %>
                  </div>
                <% end %>
              </div>
        <% end %>

<div id="status-control">
  <h4 class ="status-title">Who is in Which spot?</h4>
<div class="spot-search">
 <div class="spot-pending">
<!--       code if spot is pending  -->
  <h4 class ="status-text">Not filled</h4>
  <div class ="spot-circle">  <p class = "spot-text"><%= spot.category %> <%= spot.role %></p></div>
      <%= link_to "Search for Creatives", spot_creatives_path(spot), class: "search-creative" %>
            <svg id="arrow-dash" href="/events" viewBox="0 0 24 24">
                <polygon points="1,0 1,6 13.757,6 0.615,19.142 4.858,23.385 18,10.243 18,23 24,23 24,0"></polygon>
            </svg>

 </div>
 <div class="filled">
 <div class="spot-confirmed">
    <h4 class ="status-text-filled">Filled</h4>
    <div class ="spot-circle-filled">  <p class = "spot-text"><%= spot.category %> <%= spot.role %></p></div>
      <%= link_to "Search for creatives", spot_creatives_path(spot), class: "search-creative" %>
        <svg id="arrow-dash" href="/events" viewBox="0 0 24 24">
          <polygon points="1,0 1,6 13.757,6 0.615,19.142 4.858,23.385 18,10.243 18,23 24,23 24,0"></polygon>
        </svg>
 </div>
 </div>
</div>

<div class="event-cta">
  <div class="add-spot">
   <%= link_to "Create New spots",  new_event_spot_path(event), class: "event-dash-cta" %>
  </div>
  <div class="event-page">
     <%= link_to "View this Event Page", event_path(event), class: "event-dash-cta"%>
  </div>
  <div class="edit-event">
    <%= link_to "Edit this Event",  edit_event_path(event), class: "event-dash-cta"%>
  </div>
  <div class="cancel-event">
    <%= link_to "Cancel this Event", event_path(event), method: :delete, data: {confirm: "Are you sure?"}, class: "event-dash-cta" %>

  </div>

</div>
</div>


    <% end %>
    </div>

    <% end %>
    </div>
  </div>
</div>

